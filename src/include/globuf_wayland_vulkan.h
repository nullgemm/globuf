#ifndef H_GLOBUF_WAYLAND_VULKAN
#define H_GLOBUF_WAYLAND_VULKAN

#include "globuf.h"
#include "globuf_wayland.h"
#include "globuf_vulkan.h"

void globuf_prepare_init_wayland_vulkan(
	struct globuf_config_backend* config,
	struct globuf_error_info* error);

#endif
