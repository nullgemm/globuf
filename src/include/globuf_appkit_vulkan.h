#ifndef H_GLOBUF_APPKIT_VULKAN
#define H_GLOBUF_APPKIT_VULKAN

#include "globuf.h"
#include "globuf_appkit.h"
#include "globuf_vulkan.h"

void globuf_prepare_init_appkit_vulkan(
	struct globuf_config_backend* config,
	struct globuf_error_info* error);

#endif
